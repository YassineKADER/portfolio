{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "public" }
    }
  ],
  "env": {
    "HUGO_VERSION": "0.149.0",
    "HUGO_ENV": "production"
  },
  "buildCommand": "hugo --gc --minify"
}